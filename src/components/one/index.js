import React, { Component } from 'react'
import Two from '../two';

export default class One extends Component {
  render() {
    return (
      <div>
        one
        <Two/>
      </div>
    )
  }
}
